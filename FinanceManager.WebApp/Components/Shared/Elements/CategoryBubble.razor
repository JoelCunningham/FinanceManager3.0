@code {
    [Parameter] public CategorySummary? Category { get; set; }

    [Parameter] public bool ShowIcon { get; set; } = true;
    [Parameter] public bool IsSplit { get; set; } = false;
    [Parameter] public bool ShowGroup { get; set; } = false;
}

@if (Category is null)
{
    <p class="text-body-tertiary m-0">
        <span>Uncategorised</span>
    </p>
}
else
{
    @if (!IsSplit)
    {
        <a class="category-bubble" href="/Categories/@Category.GroupName" style="@CategoryUtilities.GetElementStyle(Category.Colour)">
            @if (ShowIcon)
            {
                <HxIcon Icon="@CategoryUtilities.GetCategroyGroupIcon(Category.GroupIcon)" />
            }
            <span>@(ShowGroup ? $"{Category.GroupName} - " : "")@Category.Name</span>
        </a>
    }
    else
    {
        <ColourDot Colour="@Category.Colour" /> 
        <span class="dot-name">@Category.Name</span>
    }
}