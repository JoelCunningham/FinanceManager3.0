@page "/Transactions"

<PageTitle>Transactions</PageTitle>

<h1 class="page-heading">Transactions</h1>

<div class="transaction-graph">
    <HxTabPanel>
        <HxTab Title="Spend by cateogry">
            <Content>
                @if (Chart2HasLargerScopedBudgets)
                {
                    <HxAlert Color="ThemeColor.Info">
                        This @Chart2.Period.ScopeText has budgets with larger scopes.
                        They will be averaged across the @Chart2.Period.ScopeText.
                    </HxAlert>
                }
                <Graph2Actions Chart="Chart2" @bind-Period="Chart2.Period" />
                <Chart Options="Chart2.Options" OnItemClick="Chart2.OnDrilldownClickAsync" />
            </Content>
        </HxTab>

        <HxTab Title="Spend over time">
            <Content>
                <Graph1Actions Chart="Chart1" @bind-Period="Chart1.Period" />
                <Chart Options="Chart1.Options" OnItemClick="Chart1.OnDrilldownClickAsync" />
            </Content>
        </HxTab>

        <HxTab Title="Chart 3">
            <Content>This is the third tab.</Content>
        </HxTab>
    </HxTabPanel>
</div>