@page "/Budget"
@inherits PageBase

<PageTitle>Budget</PageTitle>

<h1 class="page-heading">@(CurrentBudgetYear?.Year.ToString() ?? "TODO get year")</h1>

<BudgetActions EntryTypeFilter="EntryTypeFilter"
               EntryTypeFilterChanged="OnEntryTypeFilterChanged"
               EntryTypeFilters="EntryTypeFilters"
               OnPrev="Prev"
               OnToday="Today"
               OnNext="Next" />

<BudgetGrid Scope="CurrentBudgetYear?.Scope" Cells="Cells" OnCellClicked="CreateEntry" OnEntryClicked="EditEntry" />

<BudgetEntryModal @ref="_budgetEntryModal"
                  Entry="CurrentEntry"
                  CurrentBudgetYear="CurrentBudgetYear"
                  IsEditing="IsEditing"
                  Categories="Categories"
                  OnCancel="CloseEditModal"
                  OnDelete="Delete"
                  OnSave="Save" />
